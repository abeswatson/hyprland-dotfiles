###################
### KEYBINDINGS ###
###################

$terminal = ghostty
$fileManager = dolphin
$menu = rofi -show drun
$browser = zen-browser

# See https://wiki.hypr.land/Configuring/Keywords/
$su = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hypr.land/Configuring/Binds/ for more
bind = $su, RETURN, exec, $terminal
bind = $su, Q, killactive,
bind = $su, B, exec, $browser
bind = $su, M, exit,
bind = $su, E, exec, $fileManager
bind = $su, V, togglefloating,
bind = $su, T, exec, $menu
bind = $su, P, pseudo, # dwindle
# bind = $su, J, togglesplit, # dwindle

# Move focus with su + arrow keys
bind = Super, H, movefocus, l # [hidden]
bind = Super, L, movefocus, r # [hidden]
bind = Super, K, movefocus, u # [hidden]
bind = Super, J, movefocus, d # [hidden]

# Switch workspaces with su + [0-9]
bind = $su, 1, workspace, 1
bind = $su, 2, workspace, 2
bind = $su, 3, workspace, 3
bind = $su, 4, workspace, 4
bind = $su, 5, workspace, 5
bind = $su, 6, workspace, 6
bind = $su, 7, workspace, 7
bind = $su, 8, workspace, 8
bind = $su, 9, workspace, 9
bind = $su, 0, workspace, 10

# Move active window to a workspace with su + SHIFT + [0-9]
bind = $su SHIFT, 1, movetoworkspace, 1
bind = $su SHIFT, 2, movetoworkspace, 2
bind = $su SHIFT, 3, movetoworkspace, 3
bind = $su SHIFT, 4, movetoworkspace, 4
bind = $su SHIFT, 5, movetoworkspace, 5
bind = $su SHIFT, 6, movetoworkspace, 6
bind = $su SHIFT, 7, movetoworkspace, 7
bind = $su SHIFT, 8, movetoworkspace, 8
bind = $su SHIFT, 9, movetoworkspace, 9
bind = $su SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $su, S, togglespecialworkspace, magic
bind = $su SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with su + scroll
bind = $su, mouse_down, workspace, e+1
bind = $su, mouse_up, workspace, e-1

# Move/resize windows with su + LMB/RMB and dragging
bindm = $su, mouse:272, movewindow
bindm = $su, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume -l 1 @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl -e4 -n2 set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl -e4 -n2 set 5%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
